[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "space-boy"
version = "0.1.0"
description = "A lightweight 2D space arcade game built with Python and pygame."
requires-python = ">=3.8"

dependencies = [
    "pygame>=2.0.0"
]
